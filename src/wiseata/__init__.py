"""WISEATA — expansion-based artifact measurement CLI.

Wraps wise.expansion in a user-facing command line tool with two
subcommands: `expand` (produce a WiseExpansion) and `diff` (compare
two artifacts via their expansions, layer by layer).
"""

__version__ = "0.1.0"
